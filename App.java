/*
 * Written by Joseph Colangelo
 *  First version 2/17/23
 *  Current version 2/17/23
 * Graph Theory Location Nodes Project
 *  Main Class and Executable
 *      Instances of the Location classes to
 *      create the graphs
 *      Graphs are unweighted and undirected
 */

import java.util.List;

public class App {
    public static void main(String[] args) {
        /*
         * int NODE_COUNT = 15;
         * RandomSet nodeSetClass = new RandomSet(NODE_COUNT);
         * List<LocationNode> nodeSet = nodeSetClass.generateNodes();
         * for (int i = 0; i < nodeSet.size(); i++) {
         * nodeSet.get(i).printNodeData();
         * System.out.println();
         * }
         */
        int arr[] = new int[4];
        for (int i = 0; i < arr.length; i++) {
            System.out.println(arr[i]);
        }
    }
}